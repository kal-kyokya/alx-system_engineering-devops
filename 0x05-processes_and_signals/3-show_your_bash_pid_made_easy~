#!/usr/bin/env bash
# Sends to STDOUT the PID and process name of any process containing 'bash'

list=$(pgrep bash)

for process in $list
do
	pid=$(cut -d' ' f2 process)
	p_name=$(cut -d' ' f11 process)
	echo "$pid $p_name"

done
